# This is the NGINX config for madlib
# It belongs in /etc/nginx/sites-enabled
# It tells NGINX to formward traffic to flask middleware

server{
    listen 80;
    server_name madlibs.thegoldenducks.click;

    location / {
        proxy_pass http://127.0.0.1:8000;
    }
}
